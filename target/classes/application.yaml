spring:
  datasource:
    username: azureadmin
    password: ROBINantony@23
    url: jdbc:sqlserver://expensedb.database.windows.net:1433;database=expense_db;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;   
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
